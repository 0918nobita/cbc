package vision.kodai.cbc

object Compiler {
    private const val PROGRAM_NAME = "cbc"
    private const val VERSION = "1.0.0"

    private val errorHandler = ErrorHandler(PROGRAM_NAME)

    fun commandMain(args: Array<String>) {
        // TODO: porting
    }
}
